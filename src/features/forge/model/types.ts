/**
 * Card Forge authoring model — UI layer over V1 pack definitions.
 * Location: src/features/forge/model/types.ts
 */
import type { CardPresentationData } from '../../../components/cards/packPresentation';

export type ForgeCardData = CardPresentationData;
