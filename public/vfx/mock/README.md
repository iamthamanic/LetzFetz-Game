# Mock demo GLB (placeholder)

When the vfx-worker runs with `VFX_WORKER_MOCK=1`, Meshy text-to-3d returns this URL:

`/vfx/mock/demo-technique.glb`

No binary is shipped in-repo for MVP — the path is stored as `modelId` on saved TechniqueAssets. Replace with a real GLB after Meshy approval or add a tiny GLB here for local Three.js preview (#257).
