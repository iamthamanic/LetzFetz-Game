# Feature: Review, integrate, verify + docs

<!-- auto-generated by /implement on 2026-07-24 — issue #86 -->

**featureSlug:** `audio-review-integrate-docs`  
**Issue:** #86  
**Depends on:** #84, #85 (merged)

## Intent

Ship static review HTML, restrict runtime file URLs to approved assets, add `audio:verify`, write `docs/audio-system.md`, and confirm Audio Forge stays tools-only (not in the Vite bundle).

## Preconditions

- #85 generate/process + #84 audit/plan on `main`.
- Existing public assets for `card.clash` and music beds under `public/audio/`.

## Happy Path

- [ ] Static review HTML available under `tools/audio-forge/` (openable without a server).
- [ ] Runtime registry exposes `publicUrl` only for approved (file-backed) entries; planned stay null.
- [ ] `npm run audio:verify` checks manifest ↔ files ↔ registry with clear exit codes.
- [ ] `docs/audio-system.md` documents architecture, policy, forge commands, hardware limits.
- [ ] Forge/Python not in frontend bundle / package runtime deps; `npm run checks` green.

## Edge Cases

- [ ] Approved/existing ID missing file → verify fails (exit 1).
- [ ] Unapproved extra file under `public/audio/` → warn (exit 0) or documented policy.
- [ ] Review HTML works when candidates dir is empty.

## Regression

- [ ] `audio:generate` mock + `audio:process` still work.
- [ ] Music beds and card.clash still resolve via AudioManager.
- [ ] Settings/nav untouched.

## Assumptions

- Manifest status `existing` is treated as grandfathered `approved` for runtime + verify (or entries are renamed to `approved`).
- Procedural adapters remain for combat stingers until approved files exist.

## Security Coverage

| Item | Status |
|------|--------|
| F-03 secrets | PASS — no keys; forge offline |
| P-04 secrets in git | PASS |
| Bundle isolation | PASS — tools/audio-forge never imported by Vite app code |

## Screenshots

N/A — tools/docs (optional open review HTML locally).

## Implementation Notes

- Static review HTML: `tools/audio-forge/review/index.html` via `audio:review`
- Manifest + registry: file URLs only for `approved` (existing grandfathered in verify)
- `audio:verify` exit 0/1/2; forge-not-in-package.json + src import checks
- Docs: `docs/audio-system.md`; AGENTS.md reference; SOUND_POLICY approval gate
