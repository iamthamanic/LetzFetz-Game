# Feature: Shell Feature Slice

<!-- auto-generated by /implement on 2026-07-24 -->

## Intent

Ordne die App-Shell als `src/features/shell/` Slice, während `App.tsx` Composition Root bleibt und Navigation/History unverändert bleiben.

## Happy Path

- [ ] Shell-Komponenten liegen unter `features/shell/`; alte Pfade gelöscht ohne Re-Export.
- [ ] `App.tsx` importiert Shell direkt; `AppHistoryContext` bleibt shared.
- [ ] Logo-URL liegt shared unter `components/brand/` (kein Shared→Feature-Import).
- [ ] Docs/Rules zeigen neue Pfade.
- [ ] `npm run checks` + nav/smoke E2E grün.

## Screenshots

| Step | Filename |
|------|----------|
| 1 | N/A — path move |

## Implementation Notes

- Extracted `LETZ_FETZ_LOGO_SRC` to `src/components/brand/letzFetzLogo.ts` for CardBackFace.
