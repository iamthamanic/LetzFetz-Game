# Feature: Forge Feature Slice

<!-- auto-generated by /implement on 2026-07-24 -->

## Intent

Verschiebe Card Forge UI nach `src/features/forge/` mit Entry `ForgeView`, ohne Authoring-Flow oder Legacy-Supabase zu ändern.

## Happy Path

- [ ] `App.tsx` importiert `features/forge/ForgeView`; alte Forge-UI-Dateien gelöscht ohne Re-Export.
- [ ] Overlay-Storage bleibt `services/storage/cardOverlays`; `services/cardForge/{types,categories,packToForge}` bleiben bis Issue 60.
- [ ] `npm run checks` + card-forge E2E grün.

## Screenshots

| Step | Filename |
|------|----------|
| 1 | N/A — path move |

## Implementation Notes

- Renamed `CardForge` export to `ForgeView`.
