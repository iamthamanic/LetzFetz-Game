# Feature: V6 Echo / Delay Play UI

<!-- auto-generated by /implement on 2026-08-03 — issue #345 -->

## Intent
German Play UI for pending Echo/Verzögerung formulas: visible queue status plus Katalysator „bleibt bis Auflösung“ on the formula board (matches engine deferred discard). Fixed Echo amount 1 / Delay bonus +2 (no per-catalyst authoring).

## Preconditions
- Match with `v6Formula` / `meta.v6FormulaEnabled`
- Echo or Verzögerung queued via playtest hooks (#344 engine)
- Formula board rack visible (Play Formelgestell)

## Happy Path
- [ ] Pending Echo and/or Verzögerung entries visible as German queue chips (own + opponent)
- [ ] While pending: Katalysator slot shows „bleibt bis Auflösung“ (still seated on board)
- [ ] After Startphase resolve: chips clear; Katalysator badge gone; lastEvent / toast feedback readable
- [ ] Activation preview: Echo/Delay states Katalysator bleibt bis Auflösung (not „verbraucht“)
- [ ] German strings; Styleguide/Badge-style chips; `npm run checks` green

## Edge Cases
- [ ] Empty queues → no chips, no Katalysator pending badge
- [ ] Catalyst already removed before resolve → no pending badge (engine still resolves value)
- [ ] Multiple queue entries → each chip listed (engine allows append)

## Out of scope
- Per-catalyst Echo amount / Delay bonus authoring
- Slice-1 105 catalog expansion
- Konstrukte (#346)
- Default-Flip / Play-Default cutover

## Security Coverage
- F-03: UI reads engine meta queues only; no invented resolve
- P-04: local-only match state

## Regression
- [ ] Existing FormulaRig / Fessel / StatusChips unchanged when queues empty
- [ ] V5 formula path unaffected
- [ ] Engine Echo/Delay tests still pass

## Assumptions
- Echo amount fixed **1**; Delay bonus fixed **+2** (spielkonzept §8)
- Queue + seated-catalyst badge (not queue-only) — locked decision

## Screenshots
| Step | Filename |
|------|----------|
| 1 | `01-echo-queue-chips.png` |
| 2 | `02-katalysator-bleibt.png` |

## Implementation Notes
- Surface: `v6EchoDelaySurface.ts` — chips from `meta.v6EchoQueue` / `v6DelayQueue`; `pendingCatalystTiming` for seated Kat
- UI: `EchoDelayQueueChips` on FormulaRig; Katalysator badge via `FormulaSlotOccupant.pendingTiming` + FormulaSlotFace
- Preview: timingLine + „bleibt bis Auflösung“ when Echo/Delay
- Feedback: combatFeedbackCopy Echo/Verzögerung toasts from lastEvent
- Docs: spielkonzept §8 fixed 1/+2; playRulesSectionsV6 Echo/Delay line
- Tests: `v6EchoDelaySurface.test.ts`, combatFeedbackCopy + rules section smoke
- Fixed Echo amount 1 / Delay +2 — no catalog expand
