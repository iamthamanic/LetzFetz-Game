# Feature: Fetz 3D shared Engine preview for Library detail

<!-- auto-generated by /implement on 2026-07-28 — do not edit manually unless correcting intent -->

## Intent
3D-Vorschau auch in Library/Kartendetail (Forge), ohne Play→Forge-Import. Shared presentational Canvas/Assembler unter `src/components/engine3d/`; Play Panel und Forge Library mounten denselben Core.

Roadmap: Fetz 3D follow-up (Stufe B) — #145.

## Preconditions
- #143 merged: registry + GLBs for all 36 `v3-part-*` ids
- #133/#134: Play `EnginePreviewCanvas` / Assembler exist under play slice
- Forge Card Library preview modal openable

## Happy Path
- [ ] Shared Canvas/Assembler live under `src/components/engine3d/` (no Feature→Feature)
- [ ] Play `EnginePreviewPanel` imports shared canvas (Play behavior unchanged)
- [ ] Library Detail shows max one 3D canvas when card id has registry GLB
- [ ] Single-part cards preview as carrier-only (or slot-appropriate) recipe
- [ ] DE fallbacks (WebGL / missing carrier / load) remain German
- [ ] `npm run checks` green; typed-strict on touched files

## Edge Cases
- [ ] Card without registry entry → no 3D canvas (2D detail only)
- [ ] Non-Träger part still previewable (carrier-only recipe with that part id)
- [ ] WebGL missing → German fallback, no crash
- [ ] Hook exception only under `components/engine3d/three/**` (ADR D4 path updated)

## Regression
- [ ] Play Cheatbox MVP 3D panel still works
- [ ] Library Character dual-panel (front/back/ulti) unchanged for non-engine cards
- [ ] No Play→Forge or Forge→Play imports

## Assumptions
- Forge Library lists V3 engine parts (presentation kind) so detail can open a registry id
- Snapshot cache / CLI stay Play-owned under `features/play/engine3d/rendering/`

## Screenshots
| Step | Filename |
|------|----------|
| 1 | `01-library-engine-3d.png` |
| 2 | `02-play-engine-preview-still.png` (optional / e2e reuse) |

## Manual verify path
1. `npm run dev` → Card Forge → filter Fetzgerät / search `v3-part-water-traeger`
2. Open detail → 3D canvas or WebGL-DE-Fallback visible (one canvas)
3. Play → Cheatbox 3D-Assembler still opens panel

## Implementation Notes
- Files touched: `src/components/engine3d/**` (Canvas, Assembler, recipeFromPart), Play panel imports shared canvas, Forge `CardLibrary` mount + `CardKind` Engine + V3 parts in pack presentation
- Unit tests: `recipeFromPart.test.ts`, `model-utils` moved, `packToForge` Engine category
- e2e: `e2e/fetz-3d-library-preview.spec.ts` → `.qa/evidence/fetz-3d-library-preview/`
- Mount points: Play `EnginePreviewPanel` → shared canvas; Forge Library preview → `card-library-engine-3d` when registry hit
- No Feature→Feature; hook exception path → `components/engine3d/three/**`
