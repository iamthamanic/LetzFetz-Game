# Feature: V3 Fetz Trigger Paths

<!-- auto-generated by /implement on 2026-07-28 — do not edit manually unless correcting intent -->

## Intent

Wire remaining Träger/Antrieb/Aufsatz passive triggers into combat/block/activate (and High/Würfel hooks where patterns already exist), with Vitest coverage. Update the V3 gap checklist. No Three/UI bloat.

## Preconditions

- `v3Combat` + authored 36-part catalog with `effectText` / `activateCost`
- Shared `fetzCharge` pool + `fetzgeraetEffects.ts` catalog

## Happy Path

- [ ] Damage-dealt / attack-hit triggers fire once per turn via combat resolve (`onAttackHit`)
- [ ] Block triggers fire on own block resolve (`onAfterOwnBlock`)
- [ ] High gain/spend and Fokus/reroll triggers fire when those engine paths run (`onHighGainOrSpend`, `onFocusOrReroll`)
- [ ] Aufsatz pool activate still works (charge cost, no hand discard)
- [ ] Vitest covers hit, block, and at least one High/Focus path
- [ ] Gap checklist marks Teil-Trigger paths updated
- [ ] `npm run checks` green; typed-strict on touched files

## Edge Cases

- [ ] Without `v3Combat`: legacy ACTIVATE_BOUND + archetypes unchanged
- [ ] Once-per-turn flags reset on turn start (`v3FetzTriggerUsed`)
- [ ] Missing charge / exhausted part rejects activate

## Regression

- [ ] Impulse / shield / reactions still resolve
- [ ] `V3_ENGINE_PARTS_36` ID catalog still serves 3D registry (36 ids)

## Assumptions

- Restore stashed engine foundation (charge + effects catalog) onto main; do not ship UI chip work in this ticket
- Highest-value remaining hooks = High/Focus/Würfel; hit/block already patterned in effects module

## Out of Scope

- Three.js / R3F
- Confirm-UI for Träger spend
- Full prose 1:1 for every edge of all 36 parts

## Screenshots

| Step | Filename |
|------|----------|
| — | (engine-only; no UI verify required) |

## Implementation Notes

- Files touched: `fetzCharge.ts`, `fetzgeraetEffects.ts` (+ catalog), `actions.ts` (hit/block/incoming/High/Focus/status-dmg + pool activate), `engineParts36.ts` (authored defs + 3D refs), `v3-pack.ts`, gap checklist, types (`fetzCharge`, `effectText`, `activateCost`).
- Unit tests: `fetzChargeEffects.test.ts` (pool, activate, hit, High, Focus, block); `v3-pack.test.ts`; registry tests still green on 36 refs.
- Known limitations: optional Träger spend confirm UI deferred; not every exotic trigger (flip-off, discard-passive) wired into every engine path.
- Assumptions: auto-spend charge when `spendCharge` set; Fokus free-reroll only when diceRoll not forced by caller.
