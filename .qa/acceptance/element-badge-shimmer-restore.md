# Feature: Restore Material shimmer element badges on all cards

<!-- auto-generated by /implement on 2026-08-02 — do not edit manually unless correcting intent -->

## Intent
Replace GrungeElementIcon / raster element PNGs on character and card chrome with the Material ElementEffectCard shimmer badges (Lucide in black chip + light sweep). One shared component everywhere element badges appear on cards.

## Preconditions
- App open; GameSetup CharacterCarousel and Material → Effekte library available
- CSS classes `element-badge-shimmer` / `__shine` restored in `index.css`

## Happy Path
- [ ] Shared `ElementBadge` renders Lucide `ElementIcon` inside `element-badge` + `element-badge-shimmer` with German `aria-label`
- [ ] Character card parchment header uses ElementBadge (not `/icons/elements/*.png` grunge rasters)
- [ ] LetzFetzCard TCG header and Element art corner use ElementBadge for real elements
- [ ] ElementEffectCard uses the shared ElementBadge (same look as before)
- [ ] CardElementMarks / CharacterDetailPanel element rows use ElementBadge
- [ ] Shimmer sweep animates; `prefers-reduced-motion` disables shine animation

## Edge Cases
- [ ] Mystery (`??`) still renders as badge with Mysterium a11y label
- [ ] Compact `sm`/`md` cards keep readable badge scale
- [ ] Formula/Item/Glitch text role badges stay text (not forced into element icons)

## Regression
- [ ] Material Effekte grid + preview still open
- [ ] BoardCard / HandFan still render via LetzFetzCard
- [ ] Build FormulaLibraryPanel / PartLibraryPanel corner chips use ElementBadge (same shimmer)

## Assumptions
- Original shimmer CSS lived in a prior session but was never committed; restore from that pattern
- Prefer Lucide (Material) over HF rasters on card badges

## Screenshots
| Step | Filename |
|------|----------|
| 1 | `01-character-header-badge.png` |
| 2 | `02-effect-card-shimmer.png` |

## Implementation Notes
<!-- filled after coding -->
- Files touched: `src/components/ui/ElementBadge.tsx` (new), `src/index.css` (restored shimmer CSS), `ElementEffectCard.tsx`, `LetzFetzCard.tsx`, `CardElementMarks.tsx`, `CharacterDetailPanel.tsx`, `ElementEffectTile.tsx`, `ElementEffectPreviewModal.tsx`, `ElementIcon.tsx` (comments), `DESIGN.md`, `.qa/acceptance/element-badge-shimmer-restore.md`
- Restored `.element-badge-shimmer` / `__shine` + keyframes from prior Material session (CSS was referenced but missing)
- Swapped all card `variant="grunge"` ElementIcon badge slots → `ElementBadge` (Lucide + shimmer); Formula/Item/Glitch text badges unchanged
- Unit tests: none (UI chrome); `npm run build` green
- BoardCard/HandFan inherit via LetzFetzCard
