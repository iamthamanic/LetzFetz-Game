# Feature: Fetz 3D card preview wire

<!-- auto-generated by /implement on 2026-07-28 — do not edit manually unless correcting intent -->

## Intent
Kartenrahmen für Engine-Parts nutzen `previewUrl` aus der Asset-Registry; fehlt Preview → bestehendes `/cards/engine/{id}.png`. Shared `services/cardArt` + `services/engineAssets` only — kein Feature→Feature.

Roadmap: Fetz 3D follow-up (Stufe B) — Library/Forge/Board thumbs via cardArt resolver.

## Preconditions
- #143 merged: `lookupEnginePartAsset` covers all 36 `v3-part-*` ids with `previewUrl`
- Callers already use `resolveCardArtPath` for `image_asset` / LetzFetzCard portrait

## Happy Path
- [ ] Registered engine part id → `resolveCardArtPath` returns registry `previewUrl` (via `publicAssetUrl`)
- [ ] Empty / missing `previewUrl` on entry → fallback `/cards/engine/{id}.png`
- [ ] Unknown non-engine id → empty string (unchanged)
- [ ] Existing base-pack illustration paths unchanged
- [ ] Vitest covers resolver; `npm run checks` green; typed-strict on touched files

## Edge Cases
- [ ] Unknown id (not in illustration manifest, not in registry) → `''`
- [ ] Registry hit with blank `previewUrl` → PNG convention path
- [ ] No Feature→Feature imports; cardArt may import engineAssets (shared→shared)

## Regression
- [ ] Character / element / arena / glitch / ultimate art paths unchanged
- [ ] BoardCard / LetzFetzCard still consume `resolveCardArtPath` only

## Assumptions
- Library/Forge already pass card ids through `resolveCardArtPath` when building `image_asset`; this ticket does not add a new CardKind
- PNG files under `public/cards/engine/` may be absent; path convention still returned

## Screenshots
| Step | Filename |
|------|----------|
| — | n/a (resolver unit tests) |

## Implementation Notes
- Files touched: `src/services/cardArt/manifest.ts`, `manifest.test.ts`, `docs/engine-system/rendering.md`, `.qa/acceptance/fetz-3d-card-preview-wire.md`
- Unit tests: registry preview for all 36 parts; blank preview → PNG fallback; unknown id → `''`; base-pack paths unchanged
- Shared only: `cardArt` → `engineAssets.lookupEnginePartAsset` (no Feature→Feature)
