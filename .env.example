# Dev server port (default: 4789)
VITE_DEV_PORT=4789

# Optional — Meshy text→3D for `npm run asset:model -- <id> --provider=meshy`
# Never commit real keys. Opt-in flag required; fail closed without key.
# MESHY_API_KEY=
